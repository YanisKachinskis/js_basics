"use strict";

//Переменной a присваивается значение 2.
//Вычисляется выражение в скобках a = a * 2. Переменная a становится равной 4.
//Переменной x присваивается  сумма 1 + a, т.е. 1 + 4, т.е. 5.
let a = 2;
let x = 1 + (a *= 2); 